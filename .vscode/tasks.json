{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "(make) Compile debug",
            "type": "shell",
            "command": "/usr/bin/make",
            "group": "build",
            "args": [
                "-C",
                "${workspaceFolder}",
                "-j4",
            ]
        }
    ]
}