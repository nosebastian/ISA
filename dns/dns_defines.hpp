#ifndef __DNS_DEFINES_HPP

#define __DNS_DEFINES_HPP

/**
 * Maximum size of udp packet used for dns
 */
#define DNS_MAX_SIZE 512

#define DNS_MAX_LABEL_SIZE 63

#define DNS_MAX_NAME_SIZE 63

#define DNS_HEADER_SIZE 96

#endif